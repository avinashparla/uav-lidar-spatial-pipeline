# Data Management
Due to the large file size of 19M+ point clouds (~GBs), raw `.laz` and `.las` files are not hosted in this repository.

* **Sample Data**: Use the provided scripts in `src/` to download open-source sample tiles from OpenTopography.
* **Output Format**: Generated surface models are exported as GeoTIFFs using `rasterio`.
